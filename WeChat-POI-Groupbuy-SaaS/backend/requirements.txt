# 微信视频号 POI 团购 SaaS — 后端依赖
# 注意：Python 运行环境为 3.13 (cp313)，所有含 Rust/C 扩展的包必须锁定到带 cp313 预编译 wheel 的版本，
# 否则 pip 会回退到源码编译，而本机 PATH 中 Git 的 link.exe 会遮蔽 MSVC 的 link.exe 导致编译失败。
# 因此 pydantic / pydantic-core / bcrypt 均显式锁定到已发布 cp313 win_amd64 wheel 的版本。

# Web 框架
fastapi==0.110.*
uvicorn[standard]==0.29.*
# 数据校验（pydantic 2.10 依赖 pydantic-core>=2.27.2，已提供 cp313 wheel）
pydantic==2.10.6
pydantic-core==2.27.2
pydantic-settings==2.7.1
# ORM / 数据库
sqlalchemy==2.0.*
aiosqlite==0.20.*
alembic==1.13.*
# 认证 / 加密（bcrypt 4.2 提供 cp313 wheel）
pyjwt==2.8.*
bcrypt==4.2.*
python-dotenv==1.0.*
# 客户端 / 表单
httpx==0.27.*
python-multipart==0.0.9
# 测试
pytest==8.1.*
pytest-asyncio==0.23.*
